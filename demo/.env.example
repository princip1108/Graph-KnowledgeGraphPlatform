# ============================================================
# Graph+ 智汇星图 — 环境变量配置模板
# ============================================================
# 使用方法：
#   1. 复制本文件为 .env：  cp .env.example .env
#   2. 填写下方所有 <...> 占位符为实际值
#   3. .env 文件已被 .gitignore 忽略，不会提交到仓库
# ============================================================

# -------------------- 数据库 --------------------

# MySQL root 密码
# 本地开发：与你安装 MySQL 时设置的 root 密码一致
# Docker 部署：docker-compose 会用此密码自动初始化 MySQL
DB_PASSWORD=<your_mysql_root_password>

# Neo4j 密码
# 本地开发：Neo4j 首次启动后在浏览器 http://localhost:7474 设置的密码
# Docker 部署：docker-compose 会用此密码自动初始化 Neo4j
NEO4J_PASSWORD=<your_neo4j_password>

# （仅生产环境）数据库主机地址，本地开发无需设置
# DB_HOST=localhost
# DB_USERNAME=root
# NEO4J_HOST=localhost

# -------------------- GitHub OAuth2 登录 --------------------
# 获取方式：
#   1. 访问 https://github.com/settings/developers
#   2. 点击 "New OAuth App"
#   3. 填写：
#      - Application name: Graph+ 智汇星图（任意）
#      - Homepage URL: http://localhost:8080
#      - Authorization callback URL: http://localhost:8080/login/oauth2/code/github
#   4. 创建后获得 Client ID 和 Client Secret

GITHUB_CLIENT_ID=<your_github_oauth_client_id>
GITHUB_CLIENT_SECRET=<your_github_oauth_client_secret>

# -------------------- 邮件服务（163 邮箱 SMTP） --------------------
# 获取方式：
#   1. 登录 https://mail.163.com
#   2. 进入 设置 → POP3/SMTP/IMAP
#   3. 开启 SMTP 服务
#   4. 获取授权码（不是登录密码）
#
# 如果使用其他邮箱（QQ、Gmail），需要同时修改 application.yaml 中的：
#   spring.mail.host 和 spring.mail.port

MAIL_USERNAME=<your_163_email@163.com>
MAIL_AUTH_CODE=<your_163_smtp_authorization_code>

# -------------------- 文件上传路径（可选） --------------------
# 本地开发默认使用项目目录下的 uploads/ 文件夹，一般无需修改
# 服务器部署时建议设置为绝对路径，如：/data/uploads
# APP_UPLOAD_BASE_PATH=uploads

# -------------------- Spring Profile（可选） --------------------
# 切换运行环境：local（默认）/ prod
# SPRING_PROFILES_ACTIVE=local
